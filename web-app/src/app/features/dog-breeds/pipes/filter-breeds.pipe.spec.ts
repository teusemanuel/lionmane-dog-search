import { FilterBreedsPipe } from './filter-breeds.pipe';

describe('FilterBreedsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBreedsPipe();
    expect(pipe).toBeTruthy();
  });
});
