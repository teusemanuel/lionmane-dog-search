import { DetailAvatarDirective } from './detail-avatar.directive';

describe('DetailAvatarDirective', () => {
  it('should create an instance', () => {
    const directive = new DetailAvatarDirective();
    expect(directive).toBeTruthy();
  });
});
