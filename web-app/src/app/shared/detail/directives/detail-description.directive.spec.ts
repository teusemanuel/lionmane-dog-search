import { DetailDescriptionDirective } from './detail-description.directive';

describe('DetailDescriptionDirective', () => {
  it('should create an instance', () => {
    const directive = new DetailDescriptionDirective();
    expect(directive).toBeTruthy();
  });
});
