import { DetailTitleDirective } from './detail-title.directive';

describe('DetailTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new DetailTitleDirective();
    expect(directive).toBeTruthy();
  });
});
