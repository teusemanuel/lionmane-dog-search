import { EmptyContainerActionDirective } from './empty-container-action.directive';

describe('EmptyContainerActionDirective', () => {
  it('should create an instance', () => {
    const directive = new EmptyContainerActionDirective();
    expect(directive).toBeTruthy();
  });
});
